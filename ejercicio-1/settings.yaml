kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings: #necesitamos uno por cada servicio al que queramos acceder en el futuro
      - containerPort: 30001
        hostPort: 8085
        protocol: TCP